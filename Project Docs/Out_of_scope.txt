Новый модуль errors.py трансформированный в dto
сделать миграцию путей чтения конфигов и манифеста
передача DTO через DI
_ManifestReader и _ConfigReader - содержат почти одинаковые паттерны: резолв пути, чтение UTF-8, JSON-парсинг. В долгосрочной перспективе лучше выделить общий утилитарный слой (например, _JsonFileReader), чтобы избежать дублирования.
Вынеси класс инструментов (не нарушая модульности)
Документы json схема и кодовой базе устарели в чате gpt
Заместить все _pointer_from_sequence на pointer = e.json_path
"Проверка дубликатов в active_ids" извлечь из `_ConfigActiveProfileSelector` в Sainty Check.
Такое поведение может замаскировать проблему вместо fail-fast. Формирование сообщений об ошибках: В каждом raise код формирует строку вручную, добавляя config_id и type. Если в будущем изменится формат, придётся править несколько мест. Это потенциально неустойчиво.
Многие _help функции уже дублируются - _type_to_str - уникализировать
Убрать зависимоть entry: ConfigEntry и подобное - перейти к прямой передаче DI args. - как в `_ConfigActiveProfileSelector`
Проверка Дублирование ответственности между схемой и классом, Наример: JSON Schema уже гарантирует непустой массив profiles и строковый тип profile_id. Класс всё равно делает защиту (PROFILE_ARRAY_EMPTY, str() вокруг profile_id). Это нарушает принцип единой ответственности: защита от пустоты должна быть в _ConfigSanityCheck, а здесь только бизнес-логика выбора. - возможно стоит пересмотреть логику проверок в отдельную абстракцию
Провести проверку полученных классов на факт мутаций над данными
Deepcopy: copy.deepcopy(data) может быть тяжёлым для больших конфигов (особенно с большим количеством структур). При очень больших документах это станет bottleneck. Альтернатива: «ленивое копирование» только при изменении.
Класс знает о JSON Pointer: Логика построения pointer зашита в сам класс. Если в будущем JSON Pointer будет нужен в других модулях — придётся дублировать. Стоило бы вынести в утилиту.
Перегенерация DTO